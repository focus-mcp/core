{
  "name": "@focusmcp/ui",
  "version": "0.0.0",
  "description": "FocusMCP UI — dashboard Svelte (WebView Tauri ou web standalone)",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "echo 'TODO: vite build'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  }
}
